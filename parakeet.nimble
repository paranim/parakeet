# Package

version       = "0.1.0"
srcDir        = "src"
bin           = @["parakeet"]



# Dependencies

requires "nim >= 1.0.4"
requires "paranim >= 0.1.0"
requires "stb_image >= 2.5"
requires "pararules >= 0.1.0"
